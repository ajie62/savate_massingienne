{% extends 'admin/base.html.twig' %}

{% block admin_body %}
    <h1>Delete</h1>

    <p>Confirmez-vous la suppression ?</p>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">Confirm</button>
    {{ form_end(form) }}
{% endblock %}

