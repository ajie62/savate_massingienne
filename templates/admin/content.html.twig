{% extends 'admin/base.html.twig' %}

{% block admin_body %}
    <h1>Edit homepage content</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">Mettre à jour</button>
    {{ form_end(form) }}
{% endblock %}