{% extends 'admin/base.html.twig' %}

{% block admin_body %}
    <h1>Suppression d'évènement</h1>
    <p>Voulez-vous vraiment supprimer l'évènement &laquo; {{ event.name }} &raquo; ? Cette action est irréversible. </p>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <a href="{{ url('admin.event') }}">Retour</a>
    <button type="submit">Supprimer</button>
    {{ form_end(form) }}
{% endblock %}